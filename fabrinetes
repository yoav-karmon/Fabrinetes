#!/bin/bash
SCRIPT_PATH="$(readlink -f "$0")"
TASKS_DIR="$(dirname "$SCRIPT_PATH")"
LOG_DIR="$TASKS_DIR/_log"
LOGFILE="$LOG_DIR/fabrinetes_$(date +%Y-%m-%d).log"

export HDLFORGE_ORIG_PATH="$(pwd)"

# Create log directory if it doesn't exist
mkdir -p "$LOG_DIR"

# Change to the tasks directory before running invoke
cd "$TASKS_DIR" || {
    echo "[!x!]  Failed to cd into $TASKS_DIR"
    echo "run update_repo_path correctly."
    exit 1
}

# Function to show help and execute list command when no args provided
show_help_and_list() {
    echo "=== Fabrinetes Help ==="
    echo ""
    echo "Available repositories:"
    
    # Parse config file to show available containers
    if [[ -f "fabrinetes.config" ]]; then
        grep -E "^\[container\." fabrinetes.config | sed 's/\[container\.//g' | sed 's/\]//g' | while read container; do
            echo "  $container"
        done
    else
        echo "  No config file found (fabrinetes.config)"
    fi
    
    echo ""
           echo "Examples:"
           echo "  ./fabrinetes build [repo]"
           echo "  ./fabrinetes list"
           echo "  ./fabrinetes run --file fabrinetes.config --name <repository> [--rm (auto-cleanup)] [--x11 (GUI support)] [--usb (hardware access)] [--ask (confirm)] [--verbose (details)]"
           echo "  ./fabrinetes exec --container-name <name> --command '<cmd>' [--interactive]"
           echo "  ./fabrinetes shell --container-name <name>"
    echo ""
    echo "=== Executing 'list' command ==="
    echo ""
    
    # Execute the list command
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] \$ invoke -c tasks list" | tee -a "$LOGFILE"
    invoke -c tasks list | tee -a "$LOGFILE"
}

       # Check if no arguments provided
       if [[ $# -eq 0 ]]; then
           show_help_and_list
       else
           # Handle build command with repo parameter
           if [[ "$1" == "build" && -n "$2" ]]; then
               echo "[$(date '+%Y-%m-%d %H:%M:%S')] \$ invoke -c tasks build --repo=$2" | tee -a "$LOGFILE"
               invoke -c tasks build --repo="$2" | tee -a "$LOGFILE"
           # Handle exec command
           elif [[ "$1" == "exec" ]]; then
               shift  # Remove 'exec' from arguments
               echo "[$(date '+%Y-%m-%d %H:%M:%S')] \$ invoke -c tasks exec $*" | tee -a "$LOGFILE"
               invoke -c tasks exec "$@" | tee -a "$LOGFILE"
           # Handle shell command
           elif [[ "$1" == "shell" ]]; then
               shift  # Remove 'shell' from arguments
               echo "[$(date '+%Y-%m-%d %H:%M:%S')] \$ invoke -c tasks shell $*" | tee -a "$LOGFILE"
               invoke -c tasks shell "$@" | tee -a "$LOGFILE"
           else
               # Run invoke as if we were in that directory
               echo "[$(date '+%Y-%m-%d %H:%M:%S')] \$ invoke -c tasks $*" | tee -a "$LOGFILE"
               exec invoke -c tasks "$@" | tee -a "$LOGFILE"
           fi
       fi
